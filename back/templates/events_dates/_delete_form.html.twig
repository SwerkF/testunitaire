<form method="post" action="{{ path('app_events_dates_delete', {'id': events_date.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ events_date.id) }}">
    <button class="btn">Delete</button>
</form>
